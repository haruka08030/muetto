"""ソースごとのアダプタ。

どのアダプタも `list[PerfumeRecord]`（または offers）を返すところまでを担う。
ソース固有の形はここで吸収し、下流（staging 投入・マージ）には持ち込まない。
特定のソースが使えなくなっても、このディレクトリの 1 ファイルを外すだけで済むようにする
（docs/data-ingestion.md 2「ソース差し替え可能な設計」）。
"""
